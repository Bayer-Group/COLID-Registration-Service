namespace COLID.Graph.Metadata.Constants
{
    class Regex
    {
        // Regex is true if the characters are not contained in the string
        public const string InvalidUriChars = @"^[^\\|^""|^']*$";
    }
}
